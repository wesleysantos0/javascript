//  Calcular uma tmultiplicacao

function mult(x, y) {
    const multiplicacao = x * y ;
    return multiplicacao
    
}

//console.log(mult(8,2));

// calculo hora extra

function horaExtra(salario, horastrablhas) {
    const salarioPhora = (salario/220)*horastrablhas;
    console.log(salarioPhora); 
}

//horaExtra(3040, 20);

function add(soma) {
    let somar = soma + 1;
    return somar;
}
console.log(add(5));
